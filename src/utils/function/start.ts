export function start(interval: any, timeTimer: number) {

  if (timeTimer === 0) return;

  // interval = setInterval(() => {
  //   dispatch(setTimeTimer({ id: currentTask.id, time: currentTask.timeTimer - 1000 }));

  //   if (timeTimer === 0) {
  //     stop();
  //   }
  // }, 1000);


}
